// Java script is a dynamically typed language, which means that you don't have to declare the type of a variable when you create it. The type of a variable is determined at runtime based on the value assigned to it.

let age = 40;

console.log(typeof(age)); // number

age = 'thrity'
console.log(typeof(age)); //string

console.log(age);

