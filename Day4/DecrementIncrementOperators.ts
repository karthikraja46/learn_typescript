// Increment (++)
// Decrement (--)

let x_val: number = 10;

x_val++; //x_val = x_val + 1 //post increment

console.log(x_val);


//Ternary operators

let ternary_1: number = 100, ternary_2: number = 200;

let result_ternary = (number_1 > number_2) ? number_1: number_2;
console.log(result_ternary);
